# TODO

## 高優先

- 補 `user` / `system_admin` 權限邊界的整合測試與前端防呆
- 規劃 session 續期、後端登出失效與多裝置 session 管理策略
- 規劃聊天室分類後端設定 API，取代目前 `localStorage` 原型

## 中優先

- 規劃並實作設定 API，取代目前頭像 / 聊天室分類只存 `localStorage` 的前端原型
- 補聊天室分類名稱重複時的前端錯誤提示與儲存防呆
- 補搜尋選人後暫存聊天的更完整空狀態與錯誤處理
- 讓手機版與嵌入式頁面串接真實聊天資料、送訊息 API 與 WebSocket
- 建立群組建立、邀請、權限管理流程
- 補未讀訊息數完整流程與跨頁面同步細節
- 建立群組與嵌入式視窗的已讀與提醒機制
- 建立附件正式儲存策略、清理排程與留存規則
- 建立三語系字串框架（文案先以簡體中文）
- 建立訊息與檔案留存清理策略

## 低優先

- 整理桌機版左欄下拉選單 / 設定子頁互動細節，補一致的 UX 規則與狀態保存策略
- 補 repository / service 更細分層
- 補外部系統接入 / auth / whitelist / device 整合測試
- 補聊天列表、未讀數、附件訊息的前端互動測試
- 補三種前端形態的互動細節與嵌入式載入方式
- 補部署與環境變數文件

## 已完成

- 外部系統接入端點已補上 `Authorization` 以外的簽章 / timestamp 驗證
- 已補外部系統簽章規格與對接 handout 文件
- 根路徑 `/` 已可依登入狀態導向 `/login` 或 `/chat`
- `/home` 與 `/chat` 已補未登入 / session 過期導向 `/login`
- `/home` 保留入口導覽頁，`/login` 已獨立為登入頁
- 桌機版已移除舊的「建立一對一對話」入口，改由搜尋使用者建立暫存聊天
- 桌機版搜尋已可搜尋使用者與聊天記錄
- 搜尋使用者後會先開啟暫存聊天，送出第一則訊息時才建立一對一對話
- 搜尋使用者點選結果時，已可先比對既有一對一對話；若已有對話會直接載入，不再顯示未建立狀態
- 文字訊息中的 `http://` 與 `https://` 網址已可顯示為可點擊連結
- 桌機版訊息輸入區已改為附件按鈕選單，選擇圖片或文件後會開啟預覽視窗，可輸入說明文字後沿用上傳流程送出；可一次選取多個檔案，送出後保留在同一則訊息中；說明文字最多顯示六行後可捲動查看
- 桌機版對話區已支援拖拉圖片或檔案上傳，拖入時顯示文件 / 照片兩個上傳區，放開後開啟同一個預覽送出流程；多檔會以同一則訊息送出
- 桌機版訊息泡泡右鍵選單已補自己或對方訊息的引用回覆入口，並支援收回自己送出訊息的 API / WebSocket 更新流程；回覆預覽已整合在同一個輸入框區塊內，且回覆已含引用的訊息時只取該訊息本身內容；左側對話列表最後訊息預覽也只顯示回覆正文
- 設定頁已改為顯示登入帳號、個人資料與聊天室分類入口
- 已新增顯示名稱更新 API，前端編輯個人資料後可更新 `users.display_name`
- 聊天室分類與新建資料夾介面已調整為接近 Telegram Web 的流程
- 聊天室分類 `ALL` 已作為預設分類，且不顯示在自訂分類清單中供刪除 / 改名
- 聊天室分類已具備基本名稱重複過濾
- 對話列表已顯示未讀數，並有 `conversation_reads` 作為已讀狀態基礎
- 附件上傳已限制副檔名與 `40MB` 大小上限，並提供本機靜態附件存取
- `system_admin` 已禁止使用聊天 API，管理 API 已有基本角色檢查
