# agent.md

## 專案目標

本專案要建立一個以 Go 為主體的網頁聊天系統 MVP，需包含：

- 一對一聊天
- 群組聊天
- ERP 自動註冊與自動登入
- 裝置識別與登入紀錄
- 使用者個人 IP 白名單
- 電腦版與手機版 Web 介面
- 獨立登入頁、登入狀態導向與聊天頁權限保護
- 桌機版搜尋使用者 / 訊息與設定頁原型

## 開發規則

- 後端以 Go 為主，不改用其他後端語言重寫。
- 前端頁面與 API 都維持在同一個專案中。
- `system_admin` 角色不可參與聊天功能。
- 需求以 `docs/requirements.md` 為最高優先依據。
- 開發時優先延續現有檔案結構與文件，不任意重組專案。
- 功能行為、路由、API 或使用流程變更時，需同步更新 `README.md`、`TODO.md`、`docs/requirements.md`；若影響代理人工作規則或專案記憶，也需更新本檔。

## 目前已落地的介面方向

- `/` 依登入狀態導向 `/login` 或 `/chat`。
- `/home` 保留入口導覽頁，但未登入或 session 過期會導向 `/login`。
- `/login` 為獨立登入頁，登入後導向 `/chat`，登出後導向 `/login`。
- `/chat` 未登入或 session 過期會導向 `/login`。
- 桌機版左欄選單第一列顯示登入帳號，點擊後進入設定頁。
- 桌機版不再使用舊的「建立一對一對話」子頁，改由搜尋使用者開始暫存聊天。
- 搜尋使用者後會先比對既有一對一對話；若已有對話會直接載入，若尚未建立則先開啟右側暫存聊天畫面，送出第一則訊息時才建立一對一對話。
- 文字訊息支援 `http://` 與 `https://` 網址顯示為可點擊連結。
- 桌機版訊息輸入區使用附件按鈕選單選擇圖片或文件，選檔後開啟預覽視窗，可輸入說明文字並沿用既有上傳 API 送出；說明文字最多顯示六行後可捲動查看。
- 設定頁目前包含個人資料顯示、顯示名稱編輯 API、聊天室分類與資料夾 UI 原型。
- 聊天室分類目前仍以前端 `localStorage` 暫存，尚未接正式後端設定 API。

## 工作順序

1. 先閱讀 `docs/requirements.md`
2. 再閱讀 `docs/architecture.md`
3. 再閱讀 `docs/api.md`
4. 再閱讀 `docs/data-model.md`
5. 實作時同步更新 `TODO.md`
6. 若實作改變目前使用流程，同步更新 `README.md` 與需求文件狀態

## 驗證要求

- 每次修改後都要保持 `go build ./...` 可通過
- 補功能時優先維持可執行狀態
- 涉及業務邏輯時要逐步補上測試
